{
  "description": "TLA+ plugin hooks for automatic validation and configuration assistance",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If the file being written or edited has a .tla extension and is not in node_modules/ or a test directory, quickly parse it using tlaplus_mcp_sany_parse. If parse succeeds, silently continue. If parse fails, show a warning with the error message but DO NOT block the write - let it proceed. Skip this check for files larger than 100KB. This should be fast and non-blocking."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "ONLY respond if: (1) a NEW .tla file was just created (not edited), AND (2) no corresponding .cfg file exists in the same directory. If BOTH conditions are met, output ONLY this suggestion: 'No TLC configuration file found. Run /tla-symbols @file.tla to generate one?' If ANY condition is not met, output NOTHING and allow continuation silently. Do not explain why you're not responding."
          }
        ]
      }
    ]
  }
}
